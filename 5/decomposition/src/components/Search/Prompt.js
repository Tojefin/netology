import React from 'react';

/**
 * Компонент подсказка: строка под поиской строкой.
 */

export default function Prompt(props) {   
    return (
        <p className="prompt">Найдется всё. Например, фаза луны сегодня</p>
    )
}